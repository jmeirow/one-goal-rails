<h1>Executive Reporting</h1>
<h4>Access to this page limited to administrators</h4>


<table style='
      font-size:12px;
      margin-left:30px;
      border:2px solid;
      border-color:#800000;
      width:700px'>
  <tr><th colspan = 4 style='font-size:12pt;text-align:center'>Registered Member Statistics</th></tr>
  <tr> <td  style='text-align:center' colspan=4>Total Members Registered: <%= @members.count %></td></tr>
  <tr> <td>Members Requesting Buddy</td><td><%= @members.select{|x| x.wants_a_buddy == 'y'}.count %></td><td>Members Not Requesting Buddy</td><td><%= @members.select{|x| x.wants_a_buddy == 'n'}.count %></td></tr>
  <tr> <td>Members Who Have Entered a Goal</td><td><%= @members.select{|x| x.goal != 'Not yet entered'}.count %></td><td>Members Who Have Not Yet Entered a Goal</td><td><%= @members.select{|x| x.goal == 'Not yet entered'}.count %></td></tr> 
</table>

<table style='
      font-size:12px;
      margin-left:30px;
      border:2px solid;
      border-color:#800000;
      width:700px'>
  

  <tr><th colspan = 2 style='font-size:12pt;text-align:center'>Login Statistics</th></tr>
  <tr> <td  style='text-align:center' colspan=2>Total Logins:  <%= @logins.count %></td></tr>
  <tr> <td>Logins Today</td><td><%= @logins_today %></td> </tr>
  <tr> <td>Logins Last Seven Days</td><td><%= @logins_this_week %> </td></tr> 
  <tr> <td>Logins Last Thirty Days</td><td><%= @logins_this_month %> </td></tr> 
</table>


</br>

<table style='
      font-size:12px;
      margin-left:30px;
      border:2px solid;
      border-color:#800000;
      width:700px'>
  <thead>
  <tr><th colspan = 3 style='font-size:12pt;text-align:center'>Registered Members</th></tr>
  <tr><th colspan = 3 style='font-size:10pt;text-align:center'>By Registration Date/Most Recent First</th></tr>
    <tr>
      <th>Date Account Created</th>
      <th>Member</th>
      <th>Requested A Buddy</th>
    </tr>
  </thead>

  <tbody>
    <% @members.each do |member| %>
      <tr>
        <td><%= member.created_at.localtime.strftime("%m/%d/%Y %I:%M %p") %></td>
        <td><%= " #{member.last_name},  #{member.first_name}" %></td>
        <td><%= member.wants_a_buddy %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
 